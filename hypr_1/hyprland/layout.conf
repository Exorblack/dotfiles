scrolling {
    column_width = 0.62          # Adjust as needed (0.1-1.0)
    fullscreen_on_one_column = true
    follow_focus = true
    focus_fit_method = 1        # 1 = fit whole window into view
    explicit_column_widths = 0.333,0.5,0.62,0.75,1.0  # Preset widths to cycle through
}
