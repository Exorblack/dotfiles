decoration {
    rounding = 11
    rounding_power = 11

    blur {
        enabled = true
        xray = false  # Disable xray for better performance
        special = false
        new_optimizations = true
        size = 3      # Reduced from 14
        passes = 3    # Reduced from 4
        #brightness = 0.21
        noise = 0.05
        ##contrast = 0
        popups = true
        popups_ignorealpha = 1
    }

    # Shadow
    shadow {
        enabled = true
        ignore_window = true
        range = 12    # Reduced from 20
        offset = 0 2
        render_power = 2  # Reduced from 4
        color = rgba(0000002A)
    }


    # Dim
    dim_inactive = false
    dim_strength = 0
    dim_special = 0
}
